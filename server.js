const express = require('express')
const app = express()
//adding process.env.PORT that enable us that our API can be accessed by any client side code
//from anywhere or from any machine

const PORT = Process.env.PORT || 8000 ; 
